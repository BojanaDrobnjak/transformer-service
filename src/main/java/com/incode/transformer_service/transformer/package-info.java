/**
 * This package contains implementations of various transformers
 * that apply different string transformation logic.
 * <p>
 * It includes:
 * <ul>
 *   <li>{@link
 *   com.incode.transformer_service.transformer.RegexRemovalTransformer}
 *   - Transformer that removes text based on a regular expression.</li>
 *   <li>{@link
 *   com.incode.transformer_service.transformer.RegexReplaceTransformer}
 *   - Transformer that replaces text based on a regular expression.</li>
 *   <li>{@link
 *   com.incode.transformer_service.transformer.CyrillicToLatinTransformer}
 *   - Transformer that converts Cyrillic characters
 *   to their Latin equivalents.</li>
 * </ul>
 */
package com.incode.transformer_service.transformer;
